# OTLP 导出端点：443 vs 4317 vs 4318

把 OpenTelemetry 链路直接导出到后端（比如 New Relic）时，很多人会卡在端口选择上：到底用 `:4317`、`:4318`，还是干脆不写端口？

常见的误区是觉得这几个端口可以互换，"改一下端口号就行"。

**不能。** 端口绑定了协议，协议绑定了你实例化的 Exporter 类型。下面讲清映射关系，给出每种方案的 Java 代码，以及企业防火墙环境下的推荐做法。

## 一句话总结

- **4317 和 4318 不能只改端口就互换。**
- `OtlpGrpcSpanExporter` → **gRPC** → 端口 **4317** → **不需要路径**。
- `OtlpHttpSpanExporter` → **HTTP/protobuf** → 端口 **4318** → 必须带路径 **`/v1/traces`**。
- 企业内网环境下，优先用 **443 端口上的 HTTP**（不写自定义端口）。

## 端口与协议对照

| 端口 | 协议 | 传输层 | Java Exporter 类 | 端点写法 |
|------|------|--------|------------------|----------|
| 4317 | OTLP/gRPC | HTTP/2 | `OtlpGrpcSpanExporter` | `https://host:4317`（无路径） |
| 4318 | OTLP/HTTP | HTTP/1.1 | `OtlpHttpSpanExporter` | `https://host:4318/v1/traces`（必须带路径） |
| 443  | 均可 | gRPC 或 HTTP 走 443 | 任意 | `https://host`（省略端口，默认 443） |

`/v1/traces` 路径是 OTLP/HTTP 规范的一部分——HTTP Exporter 需要为不同信号类型拼接独立 URL（`/v1/traces`、`/v1/metrics`、`/v1/logs`）。gRPC **不需要**路径。

从 gRPC 切换到 HTTP 时忘记加路径，是最常见的"只改了端口就报错了"的原因。

> 大多数托管后端在同一主机上同时支持多种协议。以 New Relic 为例，`https://otlp.nr-data.net` 同时支持 gRPC、http/protobuf 和 http/json，端口 443、4317、4318 均可。根据你的账号区域选择 US 主机 `otlp.nr-data.net` 或 EU 主机 `otlp.eu01.nr-data.net`。

## 企业防火墙环境下哪个更可靠

**HTTP on 443 胜出。** 理由如下：

1. **gRPC 要求端到端 HTTP/2。** 很多公司的前向代理、TLS 解密设备和老旧防火墙不支持 HTTP/2，或者会静默降级/破坏它。gRPC 的长连接在代理空闲超时后会被断开，导致间歇性的、难以排查的导出失败。
2. **OTLP/HTTP 是普通的 HTTP/1.1 请求/响应**——所有代理都能正常处理。
3. **非常用端口（4317/4318）经常被出站规则拦截**，企业防火墙通常只放行 443。

按场景推荐：

| 场景 | 端点 | Exporter |
|------|------|----------|
| 企业防火墙内最可靠的方案 | `https://host/v1/traces`（443） | `OtlpHttpSpanExporter` |
| 想继续用 gRPC，同时减少端口问题 | `https://host`（443，省略端口） | `OtlpGrpcSpanExporter` |
| 开放网络 / 本地 Collector | `:4317` 或 `:4318` | 均可 |

---

## Java 代码示例

以下使用 OpenTelemetry Java SDK。gRPC Exporter 位于
`io.opentelemetry:opentelemetry-exporter-otlp`；HTTP Exporter 在同一包下（打包了两者）或者独立引入
`io.opentelemetry:opentelemetry-exporter-otlp-http-trace`。

### 方案 A — gRPC on 4317

```java
import io.opentelemetry.exporter.otlp.trace.OtlpGrpcSpanExporter;
import io.opentelemetry.sdk.trace.export.SpanExporter;

SpanExporter exporter = OtlpGrpcSpanExporter.builder()
        .setEndpoint("https://otlp.nr-data.net:4317")   // gRPC 不需要路径
        .addHeader("api-key", apiKey)
        .build();
```

- 开放网络上没有问题。
- 经过不支持 HTTP/2 的代理时容易出问题。

### 方案 B — gRPC on 443（省略端口）

同样的 Exporter，只是去掉端口号，默认走 443。

```java
import io.opentelemetry.exporter.otlp.trace.OtlpGrpcSpanExporter;
import io.opentelemetry.sdk.trace.export.SpanExporter;

SpanExporter exporter = OtlpGrpcSpanExporter.builder()
        .setEndpoint("https://otlp.nr-data.net")        // → 443，仍然是 gRPC
        .addHeader("api-key", apiKey)
        .build();
```

- 避开了自定义端口被拦截的问题。
- 但 gRPC/HTTP/2 在代理中的脆弱性仍然存在。

### 方案 C — HTTP on 4318

```java
import io.opentelemetry.exporter.otlp.http.trace.OtlpHttpSpanExporter;
import io.opentelemetry.sdk.trace.export.SpanExporter;

SpanExporter exporter = OtlpHttpSpanExporter.builder()
        .setEndpoint("https://otlp.nr-data.net:4318/v1/traces")  // 路径必须带
        .addHeader("api-key", apiKey)
        .build();
```

- 普通 HTTP/1.1——代理友好。
- 自定义端口仍可能被出站规则拦截 → 建议选方案 D。

### 方案 D — HTTP on 443，省略端口（防火墙内推荐）

```java
import io.opentelemetry.exporter.otlp.http.trace.OtlpHttpSpanExporter;
import io.opentelemetry.sdk.trace.export.SpanExporter;

SpanExporter exporter = OtlpHttpSpanExporter.builder()
        .setEndpoint("https://otlp.nr-data.net/v1/traces")  // 443 + 路径，HTTP/1.1
        .addHeader("api-key", apiKey)
        .build();
```

- 标准端口、标准 HTTP、不依赖 HTTP/2。
- 经过企业代理和 TLS 解密时最可靠。

### 将 Exporter 接入 TracerProvider

Exporter 的选择和 SDK 其余部分的配置无关——任何 `SpanExporter` 都可以以相同方式接入 `BatchSpanProcessor`：

```java
import io.opentelemetry.sdk.trace.SdkTracerProvider;
import io.opentelemetry.sdk.trace.export.BatchSpanProcessor;

SdkTracerProvider tracerProvider = SdkTracerProvider.builder()
        .addSpanProcessor(BatchSpanProcessor.builder(exporter).build())
        .build();
```

### 纯配置方案（无需写代码）

如果不想在代码里硬编码 Exporter，OTel SDK 支持通过环境变量配置。自动配置模块会根据 `OTEL_EXPORTER_OTLP_PROTOCOL` 自动选择对应的 Exporter：

```bash
# HTTP on 443 —— 防火墙友好默认配置
export OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf
export OTEL_EXPORTER_OTLP_ENDPOINT=https://otlp.nr-data.net
export OTEL_EXPORTER_OTLP_HEADERS=api-key=YOUR_KEY

# gRPC 等效配置
export OTEL_EXPORTER_OTLP_PROTOCOL=grpc
export OTEL_EXPORTER_OTLP_ENDPOINT=https://otlp.nr-data.net
```

使用 `OTEL_EXPORTER_OTLP_ENDPOINT`（基础 URL）时，HTTP Exporter 会自动追加 `/v1/traces`、`/v1/metrics` 等路径——你只需要给基础地址，信号路径由它自动构建。

## 常见踩坑

- **路径很关键。** gRPC 端点**不带**路径；HTTP 端点**必须带** `/v1/traces`（如果使用基础 URL 的环境变量，它会自动帮你拼接）。
- **确认区域。** 切换端点前，确认后端对应的区域主机名。
- **依赖区分。** HTTP Exporter 在 `opentelemetry-exporter-otlp-http-trace` 包中；umbrella 包 `opentelemetry-exporter-otlp` 同时包含 gRPC 和 HTTP。
- **协议不匹配报错不直观。** 把 gRPC Exporter 指向 HTTP 端口（或反过来），通常不会报"协议错误"这种清晰的提示，而是出现令人困惑的连接或解析错误——排查时先检查端口和 Exporter 的匹配关系。

## 参考资料

### New Relic
- [New Relic OTLP 端点](https://docs.newrelic.com/docs/opentelemetry/best-practices/opentelemetry-otlp) — 支持的端点、端口和必需的 Header。
- [New Relic OTLP 排查指南](https://docs.newrelic.com/docs/opentelemetry/best-practices/opentelemetry-otlp-troubleshooting) — 诊断导出失败。

### OpenTelemetry
- [OTLP Exporter 配置（SDK 环境变量）](https://opentelemetry.io/docs/languages/sdk-configuration/otlp-exporter) — `OTEL_EXPORTER_OTLP_*` 变量和按信号配置端点。
- [OpenTelemetry Protocol Exporter 规范](https://opentelemetry.io/docs/specs/otel/protocol/exporter) — OTLP/HTTP 和 OTLP/gRPC 端点与 URL 的构建方式。
- [OTLP 协议规范](https://opentelemetry.io/docs/specs/otlp/) — OTLP 协议本身（gRPC 和 HTTP 传输层）。
- [OpenTelemetry Java Exporter 配置](https://opentelemetry.io/docs/languages/java/configuration/#otlp-exporter) — Java 专属的 Exporter 配置。
